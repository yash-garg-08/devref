# SQLite (local development) — copy this file to .env and adjust
DATABASE_URL="file:./dev.db"

# PostgreSQL (production) — swap the line above for:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/devref?schema=public"
